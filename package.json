{
  "name": "cookersvdubs-site",
  "version": "1.0.0",
  "description": "Cooker's Restoration & Fabrication static site",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "node src/scripts/fetch-sanity-data.js && npx @11ty/eleventy",
    "fetch-sanity": "node src/scripts/fetch-sanity-data.js",
    "build:eleventy-only": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "@cloudinary/url-gen": "^1.22.0",
    "@sanity/client": "^7.17.0",
    "cloudinary": "^2.9.0",
    "dotenv": "^17.3.1"
  }
}
